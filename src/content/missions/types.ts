export type MissionCatalogItem = {
  id: string
  title: string
  difficulty: string
  reward: string
  objective: string
  explanation: string
  steps: string[]
  messageExample: string
  messageExampleNote: string
}
