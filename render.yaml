# OBSOLETO — não usar. Decisão de 2026-09-13: o deploy do front migrou do
# Render para o Firebase Hosting (ver firebase.json, .firebaserc,
# .github/workflows/firebase-hosting-deploy.yml e docs/02-ROADMAP.md).
# Nenhuma conta/serviço Render chegou a ser criado; este blueprint nunca
# foi aplicado. Mantido aqui só como registro histórico da configuração que
# havia sido planejada — pode ser apagado com segurança quando quiser
# (`rm render.yaml` / apagar pelo Explorer).
#
# --- conteúdo original, nunca aplicado ---
#
# services:
#   - type: web
#     name: estrelar
#     runtime: static
#     buildCommand: npm ci && npm run build
#     staticPublishPath: ./dist
#
#     routes:
#       - type: rewrite
#         source: /*
#         destination: /index.html
#
#     envVars:
#       - key: VITE_FIREBASE_API_KEY
#         sync: false
#       - key: VITE_FIREBASE_AUTH_DOMAIN
#         sync: false
#       - key: VITE_FIREBASE_PROJECT_ID
#         sync: false
#       - key: VITE_FIREBASE_STORAGE_BUCKET
#         sync: false
#       - key: VITE_FIREBASE_MESSAGING_SENDER_ID
#         sync: false
#       - key: VITE_FIREBASE_APP_ID
#         sync: false
